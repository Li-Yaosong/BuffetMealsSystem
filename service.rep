#include <QtCore>
#include "global.h"
class Service
{
    SLOT(void addClass(const Class &classInfo))

    SLOT(void delClass(const QStringList &delList))

    SLOT(void addDishes(const Dish &dishInfo))

    SLOT(void modifiDishe(const Dish &dishInfo, const QString &old))

    SLOT(void delDishes(const QStringList &delList))

    SLOT(void addOrder(const QMap<QString, QByteArray> &map))

    SLOT(QList<Order> getOrder())

    SLOT(void updateOrder(int num, int state))

    SLOT(QMap<QString, QVariant> getAllClass())

    SLOT(QMap<QString, QList<Dish>> getAllDishes())

    SLOT(void setDailyCost(const QString date, const double cost))

    SLOT(void initReport())

    SLOT(void modifiClass(const QString &newClass, const QString &old))

    SLOT(void updateReport(const QString &date, const double total))
};
