stages:
- build
Qt5.15.2-msvc2019-64bit:
  stage: build
  script:
    - mkdir build
    - cd build
    - qmake ../BuffetMealsSystem.pro -spec win32-msvc "CONFIG+=qtquickcompiler"
    - jom qmake_all
    - jom
